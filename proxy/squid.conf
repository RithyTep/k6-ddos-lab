# Squid proxy config for DDoS simulation lab
http_port 3128

# Allow all traffic (lab only!)
acl all src all
http_access allow all

# Logging
access_log stdio:/dev/stdout
cache_log stdio:/dev/stderr

# No caching (so every request hits target)
cache deny all

# Forward proxy settings
forwarded_for on
via on
